GA4:
  outputs:
    dev:
      dataset: dev
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('CGP_KEY') | fromjson }}"
      location: US
      method: service-account
      priority: interactive
      project: dbttest-435106
      threads: 4
      type: bigquery
    prod:
      dataset: prod
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('CGP_KEY') | fromjson }}"
      location: US
      method: service-account
      priority: interactive
      project: dbttest-435106
      threads: 4
      type: bigquery
    slim_ci:
      dataset: slim_ci
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('CGP_KEY') | fromjson }}"
      location: US
      method: service-account
      priority: interactive
      project: dbttest-435106
      threads: 4
      type: bigquery
  target: dev
